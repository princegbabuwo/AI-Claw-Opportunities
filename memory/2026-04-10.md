- User noted the OpenClaw environment runs in Docker.
- User's host home path is `/home/prince`, not `/home/node`.
- For Playwright/browser automation, use the Docker-safe install command:
  `docker compose run --rm --entrypoint node openclaw-cli /app/node_modules/playwright-core/cli.js install chromium`
- Do not assume `/home/node` when referring to the user's host paths; use persisted paths appropriate to their Docker setup and host layout.
