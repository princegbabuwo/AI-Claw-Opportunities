- User noted the OpenClaw environment runs in Docker.
- For Playwright/browser automation, use the Docker-safe install command:
  `docker compose run --rm openclaw-cli node /app/node_modules/playwright-core/cli.js install chromium`
- Persist Playwright browser downloads/cache under `/home/node` using `PLAYWRIGHT_BROWSERS_PATH` and a persisted home volume.
